<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="com.bandlab.metro.station.sample.SampleApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SampleApp">
        <activity
            android:name="com.bandlab.metro.station.sample.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SampleApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.bandlab.metro.station.sample.profile.ProfileActivity"
            android:exported="true"
            android:theme="@style/Theme.SampleApp" />

        <receiver
            android:name="com.bandlab.metro.station.sample.component.LoggerBroadcastReceiver"
            android:exported="false" />

        <service
            android:name="com.bandlab.metro.station.sample.component.LoggerService"
            android:exported="false" />

        <service
            android:name="com.bandlab.metro.station.sample.component.LoggerWithoutOnCreateService"
            android:exported="false" />
    </application>

</manifest>