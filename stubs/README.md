# Stubs

This module contains stubs for internal BandLab Android code. These stubs provide the minimal type signatures
needed by the compiler plugin at compile time without pulling in the actual internal dependencies. It is designed
as a JVM module to speed up test runs, you can check out samples to find out how it works in real case.

This module is not published and exists solely to support development of the compiler plugin.
