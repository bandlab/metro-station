kotlin.code.style=official

org.gradle.jvmargs=-Xmx12g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true

# Enable it if we need to switch to an internal published metro artifact
USE_INTERNAL_ARTIFACTORY=false

GROUP=com.bandlab.metro.station
VERSION_NAME=0.2.0